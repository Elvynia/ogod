{
  "name": "game-engine-driver",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/game-engine-driver/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/packages/game-engine-driver",
        "main": "packages/game-engine-driver/src/index.ts",
        "tsConfig": "packages/game-engine-driver/tsconfig.lib.json",
        "assets": [
          "packages/game-engine-driver/*.md"
        ]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs game-engine-driver {args.ver} {args.tag}"
      },
      "dependsOn": [
        {
          "target": "build"
        }
      ]
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/game-engine-driver/**/*.ts"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/packages/game-engine-driver"
      ],
      "options": {
        "jestConfig": "packages/game-engine-driver/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
